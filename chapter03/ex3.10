#include <iostream>
#include <string>
using namespace std;
int main()
{
	string s;
	cout << "Please enter a string:" << endl;
	while (getline(cin,s))
	{
		for (auto i:s)
		    if (!ispunct(i))
			    cout << i;
	}
	return 0;
}
