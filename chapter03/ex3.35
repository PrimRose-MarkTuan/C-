#include <iostream>
using namespace std;
int main()
{
	int a[10];
	int *b = begin(a);
	int *e = end(a);
	for (int *i = b; i != e; ++i)
		*i = 0;
	for (int *i = b; i != e; ++i)
		cout << *i << " ";
	cout << endl;
	return 0;
}
