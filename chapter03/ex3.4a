#include <iostream>
#include <string>
using namespace std;
int main()
{
	string str1, str2, str;
	while (cin>>str1>>str2)
	{
		if (str1 == str2)
			cout << "Two strings are equal." << endl;
		else
			cout << "Larger string is " << ((str1 > str2) ? str1 : str2) << endl;
	}
	return 0;
}
