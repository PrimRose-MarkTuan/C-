#include <iostream>
using namespace std;
void swap(int &a, int &b)
{
	int t = a;
	a = b;
	b = t;
}
int main()
{
		cout << "Please enter two numbers:" << endl;
		int a, b;
		cin >> a >> b;
		swap(a, b);
		cout << "a= " << a << " b= " << b << endl;
		return 0;
}
