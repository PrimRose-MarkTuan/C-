#include<iostream>
using namespace std;
int main()
{
  int min(int x,int y);
  int a=0,b=0,t=0;
  cout<<"Enter two numbers:"<<endl;
  cin>>a>>b;
  t=max(a,b);
  cout<<"min="<<t<<endl;
  return 0;
}
int max(int x,int y)
{
  int t;
  if(x<y)
    t=x;
  else
    t=y;
  return t;
}
